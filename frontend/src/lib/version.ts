/**
 * Single source of truth for UI package version.
 * Keep in sync with package.json and backend neuraldisc.__version__.
 */
export const APP_VERSION = "0.3.2";
export const APP_NAME = "NeuralDisc";
export const APP_VERSION_LABEL = `v${APP_VERSION}`;
